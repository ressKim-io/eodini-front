# Eodini Frontend

실시간 통학/통근 차량 위치 관리 플랫폼 - Flutter 프론트엔드

## 📋 프로젝트 개요

**Eodini**는 통학/통원 차량의 실시간 위치 추적 및 관리를 위한 모바일 중심 플랫폼입니다.

### 주요 기능
- 🚌 차량 관리 (CRUD)
- 👨‍✈️ 기사 관리
- 👶 탑승자 관리 (보호자 정보 포함)
- 🗺️ 경로 및 정류장 관리
- 📍 실시간 차량 위치 추적 (10~30초 갱신)
- 📊 운행 기록 및 일정 관리

---

## 🛠️ 기술 스택

### Core
- **Flutter** 3.x (최신 stable)
- **Dart** 3.0+

### 상태 관리
- **Riverpod** 2.4+ (상태 관리)

### 네트워킹
- **Dio** 5.4+ (HTTP 클라이언트)
- **flutter_dotenv** (환경 변수)

### 데이터 직렬화
- **freezed** (불변 모델)
- **json_serializable** (JSON 변환)

### UI/UX
- **Material Design 3**
- **google_fonts** (폰트)
- **go_router** (라우팅)

### 로컬 저장소
- **shared_preferences** (설정 저장)
- **flutter_secure_storage** (토큰 저장)

### 유틸리티
- **intl** (날짜/시간 포맷)
- **logger** (로깅)

---

## 📁 프로젝트 구조

```
lib/
├── main.dart                    # 앱 진입점
├── core/                        # 핵심 기능
│   ├── api/                     # API 통신
│   │   ├── api_client.dart      # HTTP 클라이언트
│   │   ├── api_response.dart    # 응답 모델
│   │   └── api_interceptor.dart # 인증/로깅 인터셉터
│   ├── models/                  # 데이터 모델
│   │   ├── vehicle.dart         # 차량
│   │   ├── driver.dart          # 기사
│   │   ├── passenger.dart       # 탑승자
│   │   ├── route.dart           # 경로/정류장
│   │   ├── trip.dart            # 운행 기록
│   │   └── schedule.dart        # 운행 일정
│   └── constants/               # 상수
│       ├── api_constants.dart   # API 엔드포인트
│       └── app_constants.dart   # 앱 설정
├── features/                    # 기능별 모듈 (TODO)
│   ├── auth/                    # 인증
│   ├── vehicle/                 # 차량 관리
│   ├── passenger/               # 탑승자 관리
│   └── trip/                    # 운행 관리
└── shared/                      # 공통 위젯/유틸 (TODO)
    ├── widgets/
    └── utils/
```

---

## 🚀 시작하기

### 1. 사전 준비

- **Flutter SDK** 3.0 이상 설치
- **Dart SDK** 3.0 이상
- Android Studio 또는 Xcode (플랫폼별)

### 2. 의존성 설치

```bash
flutter pub get
```

### 3. 환경 변수 설정

`.env.example`을 복사하여 `.env` 파일 생성:

```bash
cp .env.example .env
```

`.env` 파일 수정:

```env
# API 설정
API_BASE_URL=http://localhost:8080/api/v1
WS_URL=ws://localhost:8080/ws

# 환경
ENV=development
```

### 4. 코드 생성

freezed, json_serializable 코드 생성:

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

또는 watch 모드 (자동 재생성):

```bash
flutter pub run build_runner watch --delete-conflicting-outputs
```

### 5. 앱 실행

```bash
# 연결된 디바이스 확인
flutter devices

# 앱 실행
flutter run

# 특정 디바이스에서 실행
flutter run -d <device-id>
```

---

## 📡 백엔드 API

백엔드 저장소: [ressKim-io/eodini](https://github.com/ressKim-io/eodini)

### API 엔드포인트 구조

```
/api/v1
├── /auth
│   ├── POST /login
│   ├── POST /register
│   ├── GET  /me
│   └── POST /refresh
├── /vehicles
│   ├── GET    /vehicles
│   ├── POST   /vehicles
│   ├── GET    /vehicles/:id
│   ├── PUT    /vehicles/:id
│   └── DELETE /vehicles/:id
├── /drivers
├── /passengers
├── /routes
├── /trips
└── /schedules
```

### API 응답 형식

**성공 응답:**
```json
{
  "success": true,
  "message": "조회 성공",
  "data": { ... }
}
```

**에러 응답:**
```json
{
  "success": false,
  "message": "에러 메시지",
  "error": {
    "code": "ERROR_CODE",
    "message": "상세 메시지",
    "details": { ... }
  }
}
```

---

## 🔐 인증

JWT Bearer Token 방식:

```
Authorization: Bearer <access_token>
```

- Access Token: 단기 (1시간)
- Refresh Token: 장기 (7일)
- 토큰 자동 갱신 (AuthInterceptor)

---

## 📱 지원 플랫폼

- ✅ **Android** 7.0+ (API 24+)
- ✅ **iOS** 13.0+
- 🚧 **Web** (나중에 추가 예정)

---

## 🧪 테스트

```bash
# 단위 테스트
flutter test

# 통합 테스트
flutter test integration_test

# 커버리지
flutter test --coverage
```

---

## 🎭 Mock 테스트 계정

백엔드 서버 없이 앱을 테스트할 수 있도록 Mock API 모드를 제공합니다.

### Mock 모드 활성화

`lib/core/constants/app_constants.dart`에서 설정:

```dart
static const bool useMockApi = true;  // Mock API 사용
```

### 역할별 테스트 계정

Mock 모드에서는 이메일 주소에 특정 키워드를 포함하면 해당 역할로 로그인됩니다.
**비밀번호는 아무 값이나 입력 가능합니다.**

| 역할 | 이메일 예시 | 사용자 이름 | 접근 가능 기능 |
|------|------------|-------------|---------------|
| **보호자** (Parent) | parent@example.com<br/>부모@test.com | 홍학부모 | 자녀 탑승 현황, 차량 위치 추적, 알림 수신 |
| **일반 탑승자** (Passenger) | passenger@example.com<br/>승객@test.com | 이승객 | 내 운행 일정, 차량 위치 확인 |
| **운전자** (Driver) | driver@example.com<br/>기사@test.com | 김기사 | 운행 시작/완료, 탑승자 체크, 경로 확인 |
| **관리자** (Admin) | admin@example.com<br/>관리@test.com | 관리자 | 차량/기사/탑승자/경로 관리, 운행 모니터링 |
| **동승자** (Attendant) | attendant@example.com<br/>동승@test.com | 박동승 | 탑승자 체크, 안전 관리 |

### 테스트 시나리오 예시

#### 1. 보호자로 로그인
```
1. 웰컴 화면에서 "보호자" 선택
2. 이메일: parent@example.com
3. 비밀번호: (아무거나)
4. 로그인 → 보호자 홈 화면 표시
5. 자녀의 실시간 탑승 현황 확인
```

#### 2. 운전자로 로그인
```
1. 웰컴 화면에서 "운전자" 선택
2. 이메일: driver@example.com
3. 비밀번호: (아무거나)
4. 로그인 → 기사 홈 화면 표시
5. 오늘의 운행 목록에서 운행 선택
6. 운행 상세 화면에서 정류장별 탑승자 확인 및 체크
```

#### 3. 관리자로 로그인
```
1. 웰컴 화면에서 "관리자" 선택
2. 이메일: admin@example.com
3. 비밀번호: (아무거나)
4. 로그인 → 관리자 홈 화면 표시
5. 차량/기사/탑승자/경로 등 모든 관리 기능 접근
```

### 역할 전환 테스트

앱 내에서 역할을 전환하려면:
1. 우상단의 역할 전환 버튼 (↔️) 클릭
2. 로그아웃 확인
3. 웰컴 화면에서 다른 역할 선택 후 재로그인

### Mock 데이터

Mock 모드에서는 다음과 같은 샘플 데이터가 제공됩니다:
- **차량**: 3대 (45가1234, 45가5678, 45가9012)
- **운행**: 오늘 예정된 운행 2건
- **정류장**: 경로당 3-4개 정류장
- **탑승자**: 정류장당 1-3명의 어린이/학생

---

## 🏗️ 빌드

### Android APK

```bash
flutter build apk --release
```

### Android App Bundle (Google Play)

```bash
flutter build appbundle --release
```

### iOS

```bash
flutter build ios --release
```

---

## 📦 다음 단계

### 1단계: 인증 기능 구현
- [ ] 로그인 화면
- [ ] 회원가입 화면
- [ ] 토큰 관리
- [ ] 자동 로그인

### 2단계: 차량 관리
- [ ] 차량 목록 조회
- [ ] 차량 상세 정보
- [ ] 차량 등록/수정/삭제

### 3단계: 실시간 지도
- [ ] 지도 라이브러리 통합 (Naver/Kakao/Google)
- [ ] 실시간 차량 위치 표시
- [ ] 경로 및 정류장 표시

### 4단계: 탑승자 관리
- [ ] 탑승자 목록
- [ ] 탑승자 등록/수정
- [ ] 보호자 정보 관리

### 5단계: 운행 관리
- [ ] 운행 시작/완료
- [ ] 탑승/하차 체크
- [ ] 운행 기록 조회

---

## 🤝 기여

이 프로젝트는 private 저장소입니다. 팀원만 접근 가능합니다.

---

## 📄 라이선스

Proprietary - 모든 권리 보유

---

## 📞 문의

프로젝트 관련 문의: [GitHub Issues](https://github.com/ressKim-io/eodini-front/issues)
